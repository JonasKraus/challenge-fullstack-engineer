{
  "name": "@challenge/backend",
  "private": true,
  "version": "1.0.0",
  "description": "package for backend",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "test": "jest",
    "lint": "eslint 'src/*.ts'",
    "start": "node dist/index.js",
    "dev": "concurrently \"tsc --build --watch\" \"nodemon -q dist/index.js\""
  },
  "dependencies": {
    "@challenge/utils": "^1.0.0",
    "@types/cors": "^2.8.14",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.18",
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1"
  }
}
